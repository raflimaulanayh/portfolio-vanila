## Boilerplate HTML-JS-Tailwind

### Development

Please make sure you're on the ROOT folder, then install all required modules by typing:

`yarn install`

After successfully installing all required modules, you can run the dev server by running

`yarn dev`

You will see your terminal tells you the app running on port :3000, you can open your browser and see the app running.
